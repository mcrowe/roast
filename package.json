{
  "name": "roast-db",
  "description": "Immutable, transactional, database in pure Javascript.",
  "version": "0.0.4",
  "main": "lib/index.js",
  "scripts": {
    "build": "./node_modules/.bin/babel src --out-dir lib",
    "test": "./node_modules/.bin/babel src --out-dir lib && ./node_modules/.bin/babel test --out-dir test && mocha"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-plugin-transform-object-rest-spread": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "mocha": "^2.3.3"
  },
  "dependencies": {
    "lodash": "^4.0.1",
    "uuid": "^2.0.1"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/mcrowe/roast"
  }
}
